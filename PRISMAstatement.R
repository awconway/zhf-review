# can't get right dpi
PRISMAstatement::flow_exclusions(
  incl_counts = c(129, 94, 23, 15),
  total_label = "References imported for screening",
  incl_labels = c("Studies screened", "Full-text assessed for eligibility", "Included"),
  excl_labels = c("Duplicates removed", "Excluded", "Full-text excluded"))
